{
  "name": "onyx-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p rust/crates/onyx/static && npm --prefix edge-bridge ci --legacy-peer-deps && npm --prefix edge-bridge run build",
    "deploy": "mkdir -p rust/crates/onyx/static && npm --prefix edge-bridge run deploy",
    "deploy:dry-run": "npm --prefix edge-bridge run deploy:dry-run"
  },
  "dependencies": {
    "zustand": "^5.0.14"
  }
}