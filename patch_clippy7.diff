--- rust/crates/onyx/src/main.rs
+++ rust/crates/onyx/src/main.rs
@@ -1067,7 +1067,7 @@
         }
         let Some((key, value)) = arg.split_once('=') else {
             if matches!(arg.as_str(), "--resume") && index + 1 < args.len() {
-                session_reference = Some(args[index + 1].clone());
+                session_reference.clone_from(&args[index + 1]);
                 index += 2;
                 continue;
             }
@@ -4344,6 +4344,7 @@
         Ok(true)
     }

+    #[allow(clippy::too_many_lines)]
     fn handle_session_command(
         &mut self,
         action: Option<&str>,
@@ -6774,7 +6775,6 @@
                         entry.model
                     );
                     last_error = Some(error);
-                    continue;
                 }
                 Err(error) => return Err(RuntimeError::new(error.to_string())),
             }
--- rust/crates/onyx/src/render.rs
+++ rust/crates/onyx/src/render.rs
@@ -642,7 +642,19 @@
     }
 }

+struct StackEntry {
+    line_idx: usize,
+    fence: FenceLine,
+}
+
+struct Rewrite {
+    char: char,
+    new_len: usize,
+    indent: usize,
+}
+
+#[allow(clippy::too_many_lines)]
 fn normalize_nested_fences(markdown: &str) -> String {
     // A Markdown code fence is 3 or more backticks (`) or tildes (~).
     // The opening fence may have an info string (e.g. `rust`); the closing fence may not.
@@ -693,10 +705,6 @@
         }
     }

-    struct StackEntry {
-        line_idx: usize,
-        fence: FenceLine,
-    }
     // Keep track of open fences by pushing their line index and definition.
     let mut stack: Vec<StackEntry> = Vec::new();
     // If we hit an unclosed fence at EOF, we may rewrite it.
@@ -738,11 +746,6 @@
         }
     }

-    struct Rewrite {
-        char: char,
-        new_len: usize,
-        indent: usize,
-    }
     let mut rewrites: std::collections::HashMap<usize, Rewrite> = std::collections::HashMap::new();

     // First, handle EOF closure. The original logic says:
