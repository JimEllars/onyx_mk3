--- rust/crates/mock-anthropic-service/tests/scenario.rs
+++ rust/crates/mock-anthropic-service/tests/scenario.rs
@@ -106,7 +106,7 @@

     child.kill().expect("failed to kill headless server");

-    assert!(!states.is_empty(), "Headless worker should have hit the state endpoint");
+    assert!(!reported_states.is_empty(), "Headless worker should have hit the state endpoint");

-    let contains_running = states.iter().any(|s| {
+    let contains_running = reported_states.iter().any(|s| {
         if let Some(st) = s.get("status").and_then(|v| v.as_str()) {
             st == "Running"
         } else {
             false
         }
     });

     assert!(contains_running, "Worker state should transition to Running");
